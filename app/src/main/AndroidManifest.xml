<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFurnitureApp1">


        <activity android:name=".activities.NavDetailedActivity"
            android:parentActivityName=".HomeActivity" />

        <activity android:name=".activities.PlaceOrderActivity" />

        <activity android:name=".activities.NavCategoryActivity" />

        <activity
            android:name=".activities.DetailedActivity"
            android:parentActivityName=".HomeActivity" />

        <activity
            android:name=".activities.ViewAllActivity"
            android:parentActivityName=".HomeActivity" />

        <activity android:name=".activities.RegistrationActivity" />

        <activity android:name=".activities.LoginActivity" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MyFurnitureApp1.NoActionBar" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
